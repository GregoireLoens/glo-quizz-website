/**
 * Version affichée dans le footer, et seule source de vérité côté client.
 * La faire évoluer = modifier cette constante *et* poser le tag git du même nom
 * (`git tag -a v0.2 -m …`), pour que ce qui est affiché corresponde à ce qui est
 * publié sur GitHub.
 */
export const APP_VERSION = '0.6'
